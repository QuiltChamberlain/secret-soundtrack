rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /campaigns/{campaignId}/submissions/{email} {
      allow read: if false;
      allow write: if true;
    }
    
    // Default rule: block everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}